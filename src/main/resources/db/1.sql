alter table backup add "name1" TEXT;

alter table backup add "name2" TEXT;

CREATE TABLE "user11" (
"id"  INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
"name"  TEXT,
"token"  TEXT,
"theme"  TEXT,
"create_time"  TEXT,
"update_time"  TEXT
);